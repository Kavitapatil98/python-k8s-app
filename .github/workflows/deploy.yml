name: CI-CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v3

    - name: Set up Docker
      uses: docker/setup-buildx-action@v3

    - name: Build Docker Image
      run: docker build -t python-k8s-app .

    # OPTIONAL: Push to Docker Hub
    # Uncomment if you want registry push
    # - name: Login to Docker Hub
    #   uses: docker/login-action@v3
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}

    # - name: Push Image
    #   run: |
    #     docker tag python-k8s-app ${{ secrets.DOCKER_USERNAME }}/python-k8s-app:latest
    #     docker push ${{ secrets.DOCKER_USERNAME }}/python-k8s-app:latest

    - name: Install kubectl
      uses: azure/setup-kubectl@v3

    # NOTE: Local Minikube deployment won't work directly from GitHub runner
    # This step is placeholder for real cluster (EKS/GKE)

    - name: Deploy (Placeholder)
      run: echo "Deploy step will run on real cluster"